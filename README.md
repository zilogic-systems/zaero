**zearo** is a python-based open source test automation framework targetted to test 
networking devices like AP, Routers, Mesh Nodes and wireless clients.

The TAF typically has 2 major components : The Framework layer and the test-suite layer.

The framework layer is open-sourced here.